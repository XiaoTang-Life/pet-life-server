fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0
requests==2.31.0
redis>=5.0.0

# micro-life-sim生命引擎 (main分支+支持参数化周期)
# 注意：VERCEL_TOKEN 占位符会在构建时被替换
# 如果你看到这个文件，VERCEL_TOKEN 应该是环境变量
git+https://${VERCEL_TOKEN}@github.com/DeeWooo/micro-life-sim.git@main#egg=micro-life-sim

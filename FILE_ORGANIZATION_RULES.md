# 文件组织规则

**日期**: 2025-11-03
**状态**: ✅ 强制执行
**目的**: 保持项目整洁和可维护性

---

## 🎯 总体原则

1. **根目录最小化** - 只放核心文件
2. **分类明确** - 文档、测试、脚本各有目录
3. **命名一致** - 遵循统一的命名规范
4. **易于查找** - 文件位置符合直觉

---

## 📋 强制规则

### ✅ 根目录规则

**只允许**以下文件在根目录：

| 文件类型 | 允许的文件 | 说明 |
|---------|----------|------|
| **核心代码** | `main.py` | FastAPI 应用入口 |
| **配置文件** | `requirements.txt`, `vercel.json`, `.gitignore` | 项目配置 |
| **环境变量** | `.env`, `.env.local` | 本地配置（不提交） |
| **README** | `README.md` | 项目主入口文档 |
| **快速指南** | `QUICKSTART.md` | ⚠️ 仅此一个文档 |

**❌ 禁止**放在根目录：
- ❌ 其他 Markdown 文档（如 `INTEGRATION_GUIDE.md`）→ 移到 `docs/guides/`
- ❌ 测试脚本（如 `test_*.py`）→ 移到 `tests/`
- ❌ 工具脚本（如 `build.sh`）→ 移到 `scripts/`
- ❌ 临时文件和日志

---

## 📂 目录结构规范

### 1. `docs/` - 所有文档

**用途**: 存放所有项目文档，按类型和阶段分类

```
docs/
│
├── README.md                           # ⭐ 文档导航中心（必须）
│
├── guides/                             # 📖 使用指南和教程
│   ├── integration-guide.md            # 客户端集成指南
│   ├── vercel-setup.md                 # Vercel 部署教程
│   └── upstash-setup.md                # Upstash 安装教程
│
├── references/                         # 📝 参考文档和总结
│   ├── upstash-quick-reference.md      # 快速参考
│   └── upstash-integration-summary.md  # 集成总结
│
├── Phase-N-[功能名]/                   # 阶段级文档（按时间顺序）
│   ├── README.md                       # 阶段导航
│   ├── [功能]-设计方案.md               # 技术方案
│   ├── [功能]-实施指南.md               # 部署手册
│   └── [功能]-测试报告.md               # 验证结果
│
├── ivy/                                # 📋 研究与设计（已归档）
│   └── ...早期规划文档
│
├── 整体重构总结.md                     # 项目级总结
└── 验证测试报告.md                     # 项目级测试报告
```

**新增文档时的决策树**：

```
新建文档 →
├─ 是使用指南/教程？ → docs/guides/[文件名].md
├─ 是参考文档/总结？ → docs/references/[文件名].md
├─ 是阶段性工作文档？ → docs/Phase-N-[功能名]/[文件名].md
└─ 是项目级文档？ → docs/[文件名].md
```

**文档命名规范**：
- 使用 kebab-case：`integration-guide.md`（英文）
- 或使用中文：`整体重构总结.md`
- 避免空格和特殊字符

### 2. `tests/` - 所有测试

**用途**: 存放所有测试脚本

```
tests/
├── test_api.py                         # API 端点测试
├── test_integration.py                 # 集成测试（端到端）
├── test_redis_connection.py            # Redis 连接测试
└── test_vercel.py                      # Vercel 部署测试
```

**命名规范**：
- 必须以 `test_` 开头
- 使用小写和下划线：`test_feature_name.py`
- 一个文件测试一个模块或功能

**运行测试**：
```bash
# 运行所有测试
python3 -m pytest tests/

# 运行单个测试
python3 tests/test_api.py
```

### 3. `scripts/` - 构建和部署脚本

**用途**: 存放所有自动化脚本

```
scripts/
├── build.sh                            # 主构建脚本
├── build-alternative.sh                # 替代构建方案
├── install-deps.sh                     # 安装依赖
└── prepare-build.sh                    # 准备构建环境
```

**命名规范**：
- 使用 kebab-case：`script-name.sh`
- 使用 `.sh` 扩展名（Bash 脚本）
- 添加执行权限：`chmod +x scripts/*.sh`

**使用示例**：
```bash
# 安装依赖
bash scripts/install-deps.sh

# 构建项目
bash scripts/build.sh
```

### 4. `src/` - 源代码

**用途**: 存放所有 Python 源代码

```
src/
├── __init__.py
├── life_adapter.py                     # Life 引擎适配层
├── models.py                           # 数据模型定义
└── pet_adapter.py                      # 宠物适配器
```

**命名规范**：
- 使用 snake_case：`life_adapter.py`
- 每个文件一个主要类或模块
- 添加 `__init__.py` 标记为 Python 包

### 5. `api/` - Vercel API 路由

**用途**: Vercel 无服务器函数

```
api/
├── __init__.py
└── index.py                            # API 入口（Vercel Functions）
```

**规范**：
- 遵循 Vercel Functions 命名规范
- 每个文件对应一个 API 端点

---

## 🚨 违反规则的处理

### 检查清单

在提交代码前，检查：

```bash
# 检查根目录是否有多余文件
ls -la | grep -E "\\.md$|test_.*\\.py$|\\.sh$"

# 应该只看到 README.md 和 QUICKSTART.md
```

### 重构指南

如果发现违反规则的文件：

1. **移动文档**：
   ```bash
   git mv <旧位置>.md docs/guides/<新位置>.md
   ```

2. **移动测试**：
   ```bash
   git mv test_*.py tests/
   ```

3. **移动脚本**：
   ```bash
   git mv *.sh scripts/
   ```

4. **更新引用**：
   - 搜索所有引用旧路径的文件
   - 更新为新路径

---

## 📝 文档内容规范

### 文档头部格式

每个文档必须包含清晰的元数据：

```markdown
# 文档标题

**文档类型**: 使用指南 / 参考文档 / 阶段文档 / 项目文档
**项目**: XiaoTang-Life / pet-life-server
**阶段**: Phase N - [阶段描述]（如果适用）
**日期**: YYYY-MM-DD
**状态**: ✅ 完成 / ⏳ 进行中 / 🔄 待更新

> 一句话描述本文档的目的和内容

---
```

### 链接规范

- 使用相对路径：`[文档](docs/guides/integration-guide.md)`
- 添加锚点链接：`[章节](#section-name)`
- 文档间相互引用

---

## 📦 gitignore 规则

确保以下文件不被提交：

```gitignore
# 环境变量
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# IDE
.vscode/
.idea/
*.swp

# 临时文件
*.log
tmp/
.DS_Store
```

---

## ✅ 检查清单（用于代码审查）

在创建 Pull Request 前：

- [ ] 根目录只有核心文件（README, QUICKSTART, main.py, 配置文件）
- [ ] 所有文档都在 `docs/` 目录下
- [ ] 所有测试都在 `tests/` 目录下
- [ ] 所有脚本都在 `scripts/` 目录下
- [ ] 文件命名符合规范
- [ ] 文档包含完整的元数据头部
- [ ] 更新了相关的 README 或导航文件

---

## 🔄 迁移记录

### 2025-11-03 - 初次整理

**移动的文件**：

```
根目录 → docs/guides/
  - INTEGRATION_GUIDE.md → docs/guides/integration-guide.md
  - VERCEL_SETUP.md → docs/guides/vercel-setup.md
  - UPSTASH_SETUP.md → docs/guides/upstash-setup.md

根目录 → docs/references/
  - UPSTASH_INTEGRATION_SUMMARY.md → docs/references/upstash-integration-summary.md
  - UPSTASH_QUICK_REFERENCE.md → docs/references/upstash-quick-reference.md

根目录 → tests/
  - test_api.py → tests/test_api.py
  - test_integration.py → tests/test_integration.py
  - test_redis_connection.py → tests/test_redis_connection.py
  - test_vercel.py → tests/test_vercel.py

根目录 → scripts/
  - build.sh → scripts/build.sh
  - build-alternative.sh → scripts/build-alternative.sh
  - install-deps.sh → scripts/install-deps.sh
  - prepare-build.sh → scripts/prepare-build.sh
```

**保留在根目录**：
- ✅ README.md
- ✅ QUICKSTART.md
- ✅ main.py
- ✅ requirements.txt
- ✅ vercel.json

---

## 📚 参考

- [README.md](README.md) - 项目主文档
- [docs/README.md](docs/README.md) - 文档导航中心
- [QUICKSTART.md](QUICKSTART.md) - 快速开始指南

---

**最后更新**: 2025-11-03
**维护者**: Claude Code
**状态**: ✅ 强制执行

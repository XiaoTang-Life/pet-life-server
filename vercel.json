{
  "version": 2,
  "buildCommand": "bash -c 'sed -i.bak \"s|\\${GITHUB_TOKEN}|$GITHUB_TOKEN|g\" requirements.txt && vercel-build'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.py"
    }
  ]
}

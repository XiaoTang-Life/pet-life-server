{
  "version": 2,
  "buildCommand": "bash -c 'sed -i.bak \"s|\\${VERCEL_TOKEN}|$VERCEL_TOKEN|g\" requirements.txt && vercel-build'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.py"
    }
  ]
}
